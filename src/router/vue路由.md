# vue路由

## 结构

```text
├── router                 //vue路由
│   ├── guard              // 路由守卫
│   ├── helpers            // 路由相关的辅助函数
│   ├── modules            // 声明动态路由
│   └── routes             // 声明的固定路由
```
